import {store} from "../core/store.js";import {$,esc,money,toast} from "../core/ui.js";import {save,remove} from "../core/supabase.js";
export function fillObraSelects(){const c=store.clientes.map(x=>`<option value="${x.id}">${esc(x.nome)}</option>`).join("");obraClienteId.innerHTML='<option value="">Selecionar</option>'+c}
export function renderObras(rows=store.obras){$("obrasTable").innerHTML=rows.length?`<table><thead><tr><th>Obra</th><th>Cliente</th><th>Estado</th><th>Valor</th><th>Progresso</th><th>Ações</th></tr></thead><tbody>${rows.map(o=>`<tr><td>${esc(o.nome)}</td><td>${esc(o.clientes?.nome||"")}</td><td>${esc(o.estado||"")}</td><td>${money(o.valor_contratado)}</td><td>${o.progresso||0}%</td><td><button class="btn small light" data-edit-obra="${o.id}">Editar</button> <button class="btn small danger" data-del-obra="${o.id}">Apagar</button></td></tr>`).join("")}</tbody></table>`:"<p>Sem obras.</p>"}
export function openObra(o={}){fillObraSelects();obraId.value=o.id||"";obraClienteId.value=o.cliente_id||"";obraNome.value=o.nome||"";obraMorada.value=o.morada||"";obraEstado.value=o.estado||"Orçamento";obraValor.value=o.valor_contratado||"";obraProgresso.value=o.progresso||0;obraDialog.showModal()}
export async function submitObra(e,refresh){e.preventDefault();try{await save("obras",{cliente_id:obraClienteId.value,nome:obraNome.value,morada:obraMorada.value||null,estado:obraEstado.value,valor_contratado:Number(obraValor.value||0),progresso:Number(obraProgresso.value||0)},obraId.value||null);obraDialog.close();toast("Obra guardada.");await refresh()}catch(err){toast(err.message,"error")}}
export async function deleteObra(id,refresh){if(!confirm("Confirmar eliminação?"))return;try{await remove("obras",id);toast("Obra apagada.");await refresh()}catch(err){toast(err.message,"error")}}
