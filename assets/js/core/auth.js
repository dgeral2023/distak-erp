import {db,getProfile} from "./supabase.js";export async function login(email,password){const {data,error}=await db.auth.signInWithPassword({email,password});if(error)throw error;return {user:data.user,profile:await getProfile(data.user.id)}}export async function logout(){await db.auth.signOut()}export async function session(){const {data}=await db.auth.getSession();if(!data.session)return null;return {user:data.session.user,profile:await getProfile(data.session.user.id)}}